# Modelos

Esta carpeta almacena modelos de IA para el proyecto.

## Directorios

- `checkpoints/` — checkpoints de entrenamiento
- `fine_tuned/` — modelos ajustados (fine-tuned)

## Modelos

Pendiente: documentar los modelos y versiones utilizados.